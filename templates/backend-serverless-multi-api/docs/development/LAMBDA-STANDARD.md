# Lambda Standard

## Handler convention

- Validate headers, path params, query params and body at the edge.
- Delegate business rules to services.
- Return consistent JSON responses.
- Log request id, function name and meaningful domain context.
- Do not leak secrets or PII into logs.

