service: __PROJECT_SLUG__-layer-email

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs20.x
  region: ${opt:region, '__AWS_REGION__'}

layers:
  email:
    path: nodejs
    name: ${self:service}-${sls:stage}
    description: Shared email provider adapters.
