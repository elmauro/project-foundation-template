service: __PROJECT_SLUG__-layer-engine

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs20.x
  region: ${opt:region, '__AWS_REGION__'}

layers:
  engine:
    path: nodejs
    name: ${self:service}-${sls:stage}
    description: Shared business rules or decision engine helpers.
