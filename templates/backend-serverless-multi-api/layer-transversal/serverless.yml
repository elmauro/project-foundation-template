service: __PROJECT_SLUG__-layer-transversal

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs20.x
  region: ${opt:region, '__AWS_REGION__'}

layers:
  transversal:
    path: nodejs
    name: ${self:service}-${sls:stage}
    description: Shared controllers, errors, logging and response helpers.
