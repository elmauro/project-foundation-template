service: __PROJECT_SLUG__-otp-api

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs20.x
  region: ${opt:region, '__AWS_REGION__'}
  stage: ${opt:stage, 'dev'}

functions:
  handlingOtp:
    handler: functions/handlingOtp/handler.handler
    events:
      - httpApi:
          method: POST
          path: /api/otp
