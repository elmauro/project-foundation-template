# User Story — `<feature-name>`

> Use Feature name in the title. Use Feature slug only in the `Slug:` field and folder path.

## Title

- Name: `<feature-name>`
- Slug: `<feature-slug>`
- Ticket/story:
- Stack scope: backend | frontend | infrastructure | full-stack

---

## Suggested sizing

- Story points (Fibonacci: 1, 2, 3, 5, 8, 13): `<n | n/a>`
- Indicative only; re-estimate during planning.
- Rationale: `<one line: stacks touched, contract/data risk, unknowns>`

---

## Goal

- As a `<user role or system actor>`
- I want `<capability>`
- So that `<business value>`

---

## SOURCE SCOPE

- Current behavior included:
  - `<behavior>` — evidence: `<repo path / doc / endpoint / component>`
- Current behavior excluded:
  - `<behavior>` — reason: deferred | out of scope | unknown
- Source docs:
  - `<doc/path>` — role:

---

## TARGET SCOPE

- Backend target (`backend/`):
  - API / function:
  - service / layer:
  - data / infra:
- Frontend target (`frontend/`):
  - page / route:
  - component:
  - service / type:
- Infrastructure target (`infrastructure/`):
  - module / capability:
  - env / IAM:
- Out of scope:
  - `<item>` — reason:

---

## FLOW

1. `<user/system step>` — expected target: `<behavior>` — evidence/source: `<path/doc>`
2. `<user/system step>` — expected target: `<behavior>` — evidence/source: `<path/doc>`
3. `<user/system step>` — expected target: `<behavior>` — evidence/source: `<path/doc>`

---

## Acceptance Criteria

1. `<testable criterion>` — validation: backend | frontend | E2E | manual
2. `<testable criterion>` — validation: backend | frontend | E2E | manual
3. `<testable criterion>` — validation: backend | frontend | E2E | manual

---

## GAPS

- Deferred acceptance:
  - `<gap>` — reason:
- Unknowns:
  - `<unknown>` — evidence checked:

---

## RISKS

- Product / contract risks:
  - `<risk>` — mitigation/check:
- Validation focus:
  - `<scenario>` — expected result:

---

## Notes

- Environment / flags:
- Test data:
- Related docs:
- Last updated:
