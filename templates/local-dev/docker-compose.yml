services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: __PROJECT_SLUG__
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:

